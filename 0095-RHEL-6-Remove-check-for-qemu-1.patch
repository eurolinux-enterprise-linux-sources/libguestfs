From fd85ec6d4639db8e750565032e95cd7e65b6b2a7 Mon Sep 17 00:00:00 2001
From: "Richard W.M. Jones" <rjones@redhat.com>
Date: Fri, 17 May 2013 22:26:34 +0100
Subject: [PATCH 095/102] RHEL 6: Remove check for qemu >= 1.

---
 configure.ac | 8 --------
 1 file changed, 8 deletions(-)

diff --git a/configure.ac b/configure.ac
index e23585e..4641f43 100644
--- a/configure.ac
+++ b/configure.ac
@@ -698,14 +698,6 @@ working.
 ])
     fi
 
-    AC_MSG_CHECKING([for $QEMU version >= 1])
-    if $QEMU -version | grep -sq 'version @<:@1-@:>@'; then
-        AC_MSG_RESULT([yes])
-    else
-        AC_MSG_RESULT([no])
-        AC_MSG_FAILURE([$QEMU version must be >= 1.0.])
-    fi
-
     AC_MSG_CHECKING([that $QEMU -nographic -machine accel=kvm:tcg -device ? works])
     if $QEMU -nographic -machine accel=kvm:tcg -device \? >&AS_MESSAGE_LOG_FD 2>&1; then
         AC_MSG_RESULT([yes])
-- 
1.9.3

